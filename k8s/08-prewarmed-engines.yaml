apiVersion: apps/v1
kind: Deployment
metadata:
  name: prewarmed-cpp-engine
  namespace: eci-sandboxes
  labels:
    app: pre-warmed-sandbox
    env_type: cpp
spec:
  replicas: 5
  selector:
    matchLabels:
      app: pre-warmed-sandbox
      env_type: cpp
  template:
    metadata:
      labels:
        app: pre-warmed-sandbox
        env_type: cpp
    spec:
      containers:
        - name: secure-engine
          image: eci-cpp-engine:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          resources:
            requests:
              memory: "64Mi"
              cpu: "20m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          volumeMounts:
            - mountPath: /tmp
              name: ram-disk
      volumes:
        - name: ram-disk
          emptyDir:
            medium: Memory
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prewarmed-python-engine
  namespace: eci-sandboxes
  labels:
    app: pre-warmed-sandbox
    env_type: python
spec:
  replicas: 5
  selector:
    matchLabels:
      app: pre-warmed-sandbox
      env_type: python
  template:
    metadata:
      labels:
        app: pre-warmed-sandbox
        env_type: python
    spec:
      containers:
        - name: secure-engine
          image: eci-python-engine:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          resources:
            requests:
              memory: "64Mi"
              cpu: "20m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          volumeMounts:
            - mountPath: /tmp
              name: ram-disk
      volumes:
        - name: ram-disk
          emptyDir:
            medium: Memory
---
# ==========================================
# JVM Engine (Java / Kotlin)
# ==========================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prewarmed-jvm-engine
  namespace: eci-sandboxes
  labels:
    app: pre-warmed-sandbox
    env_type: jvm
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pre-warmed-sandbox
      env_type: jvm
  template:
    metadata:
      labels:
        app: pre-warmed-sandbox
        env_type: jvm
    spec:
      containers:
        - name: secure-engine
          image: eci-jvm-engine:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"   # JVM needs more RAM than native engines
              cpu: "500m"
          volumeMounts:
            - mountPath: /tmp
              name: ram-disk
      volumes:
        - name: ram-disk
          emptyDir:
            medium: Memory
---
# ==========================================
# .NET Engine (C#)
# ==========================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prewarmed-dotnet-engine
  namespace: eci-sandboxes
  labels:
    app: pre-warmed-sandbox
    env_type: dotnet
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pre-warmed-sandbox
      env_type: dotnet
  template:
    metadata:
      labels:
        app: pre-warmed-sandbox
        env_type: dotnet
    spec:
      containers:
        - name: secure-engine
          image: eci-dotnet-engine:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "384Mi"
              cpu: "500m"
          volumeMounts:
            - mountPath: /tmp
              name: ram-disk
      volumes:
        - name: ram-disk
          emptyDir:
            medium: Memory
